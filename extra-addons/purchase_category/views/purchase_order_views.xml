<odoo>
    <!--aqui insertamos dentro de un modelo ya existe en nuevo campo, siempre y caundo el state sea purchase -->
    <record id="view_purchase_order_category" model="ir.ui.view">
        <field name="name">purchase.order.form.category</field>
        <field name="model">purchase.order</field>
        <field name="inherit_id" ref="purchase.purchase_order_form"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='partner_ref']" position="after">
                <field name="purchase_category_id"
                       attrs="{'invisible': [('state', '!=', 'purchase')]}"/>
            </xpath>
        </field>
    </record>
</odoo>
